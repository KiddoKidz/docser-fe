import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  root: {
    textAlign: "center",
    color: "#A1A1A1",
    fontWeight: "500",
  },
  list: {
    textAlign: "left",
    maxWidth: 300,
  },
}));
