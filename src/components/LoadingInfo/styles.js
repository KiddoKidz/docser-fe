import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  item: {
    alignItems: "center",
    justify: "center",
    fontWeight: "500",
    color: "#A1A1A1",
    textAlign: "center",
  },
}));
